🟢 1. asyncio01.py → Queue เบื้องต้น
Producer สร้างงาน 10 ชิ้น (ค่า 0–9) แล้ว await queue.put(value) เพื่อใส่ลง Queue
หลังเสร็จจะส่งสัญญาณ None เพื่อบอก consumer ให้หยุด
Consumer รอด้วย await queue.get() → ได้ค่าแล้ว print ออกมา → ถ้าเจอ None ก็ break
📌 ตรงนี้เหมือนตัวอย่าง Producer-Consumer ใน PDF (หน้าแรก ๆ) → ใช้ put() และ get() ซึ่งเป็น blocking แบบ async → ถ้า Queue ว่าง consumer จะ “รอ” จน producer ใส่ของเข้ามา

🟢 2. asyncio02.py → Queue แบบไม่บล็อก
ใช้ queue.get_nowait() แทน await queue.get()
ถ้า Queue ว่าง → จะเกิด asyncio.QueueEmpty → จับด้วย try-except แล้ว consumer พัก await asyncio.sleep(0.5)
เหมาะกับกรณีที่ consumer ไม่อยาก “บล็อก” แต่จะวนเช็คเรื่อย ๆ (busy waiting + sleep)
📌 ตรงนี้เชื่อมกับหัวข้อ Asyncio Queue without block ที่บอกว่า consumer อาจใช้ QueueEmpty exception แทนการรอ

🟢 3. asyncio03.py → Queue + task_done() และ join()
Producer ใส่ค่าลง Queue แล้วจบ
Consumer ดึงด้วย await queue.get() → หลังประมวลผลเสร็จ ต้อง queue.task_done() เพื่อบอกว่า “งานชิ้นนี้เสร็จแล้ว”
await queue.join() ใน main จะรอจนทุกงานถูก consumer ทำครบ
📌 ตรงนี้ตรงกับหัวข้อ Asyncio Queue Join and Task done ที่ใช้ join/task_done เพื่อ sync ว่าทุก task เสร็จแล้วจริง ๆ

🟢 4. asyncio04.py → Supermarket Simulation
Customer (Producer) → ใส่ (ชื่อ, สินค้า) ลง queue
Cashier (Consumer) → หยิบลูกค้าออกมา → คำนวณเวลาตามจำนวนสินค้า → รอด้วย await asyncio.sleep(process_time) → เสร็จแล้ว task_done()
มีหลาย cashier ทำงานพร้อมกัน (ขนานจริงด้วย asyncio)
เมื่อ customer ทุกคนเสร็จ → queue.join() → จากนั้นยกเลิก task ของ cashier
สุดท้ายสรุปสถิติแต่ละ cashier
📌 ตรงกับตัวอย่าง Supermarket Simulation ที่อธิบายว่า queue คือ buffer ระหว่าง producer (ลูกค้า) และ consumer (แคชเชียร์) เพื่อ balance กัน

✅ สรุปการเรียนรู้จาก 4 ไฟล์นี้
asyncio.Queue() ใช้เป็น buffer ระหว่าง coroutines
put() / get() → ใช้กับ async, ถ้า queue ว่าง/เต็มจะบล็อก (รอ)
get_nowait() / put_nowait() → แบบไม่บล็อก แต่ต้องจับ exception
task_done() / join() → ใช้ sync ว่า consumer ทำงานครบแล้ว
Pattern นี้ = Producer-Consumer → สำคัญมากใน high-concurrency

